<%= turbo_frame_tag flashcard do%>
  <%= form_with model: flashcard, url: toggle_flashcard_path(flashcard), local: false, remote: true do |f| %>
  <%= f.check_box :checkbox, onchange: "event.preventDefault(); this.form.requestSubmit();" %>
  <% end %>
<% end %>